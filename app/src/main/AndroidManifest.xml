<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.StageMate"
        android:usesCleartextTraffic="true"
        android:localeConfig="@xml/locales_config"
        tools:targetApi="31">


        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <!-- Splash Activity (LAUNCHER) -->
        <activity
            android:name=".ui.activities.SplashActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Onboarding Activity -->
        <activity
            android:name=".ui.activities.OnboardingActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Login Activity -->
        <activity
            android:name=".ui.activities.LoginActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- SignUp Activity -->
        <activity
            android:name=".ui.activities.SignUpActivity"
            android:exported="false"
            android:screenOrientation="portrait" />



        <!-- Preferred Categories Activity -->
        <activity
            android:name=".ui.activities.PreferredCategoriesActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Main Activity -->
        <activity
            android:name=".ui.activities.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <!-- Deep Links for events -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="stagemate.app"
                    android:pathPrefix="/event" />
            </intent-filter>
            <!-- App Links (custom scheme) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="stagemate"
                    android:host="event" />
            </intent-filter>
        </activity>

        <!-- Event Detail Activity -->
        <activity
            android:name=".ui.activities.EventDetailActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <!-- Deep Links for specific events -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="stagemate.app"
                    android:pathPattern="/event/.*" />
            </intent-filter>
        </activity>


        <!-- Payment Activity -->
        <activity
            android:name=".ui.activities.PaymentActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Receipt Activity -->
        <activity
            android:name=".ui.activities.ReceiptActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Personal Info Activity -->
        <activity
            android:name=".ui.activities.PersonalInfoActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Security Activity -->
        <activity
            android:name=".ui.activities.SecurityActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Help Activity -->
        <activity
            android:name=".ui.activities.HelpActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Privacy Policy Activity -->
        <activity
            android:name=".ui.activities.PrivacyPolicyActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- 🆕 Seat Selection Activity -->
        <activity
            android:name=".ui.activities.SeatSelectionActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.activities.TicketDetailActivity"
            android:exported="false"
            android:screenOrientation="portrait" />


        <!-- Firebase Cloud Messaging Service -->
        <service
            android:name=".services.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Event Reminder BroadcastReceiver -->
        <receiver
            android:name=".services.EventReminderReceiver"
            android:exported="false" />

    </application>


</manifest>
